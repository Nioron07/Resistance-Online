This is just a very simple, vibe coded frontend so I could test auth during development without touching the actual frontend.

To run it, run `npm install` then `npx serve -l <port>` with the port of your choice.

- Joseph Habisohn 3/2/2026